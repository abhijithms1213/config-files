##  Sunday

### Key Things I Learned Today
-  in neovim default file explorer is netrw  
	- :Ex for explorer open, :e for edit a afile, % is for new file create 
 ```lua
--swap
local v1,v2,temp=2,10,nil
temp=v1
v1=v2
v2=temp
print("swapped v1 and v2",v1,v2)

--swap without 3rd variable
local a,b=20,60
a= a+b//80
b= a-b//20
a=a-b//60
 
 ```

- swap values with and without temp variable
- #if_syntax 
- #lua_star 
- bash
	- 

### Insights / Realizations
- 20 min study 10 min prepare what u learned 
	- write down in obisidian don't push too much to learn , and don't compare with others
		- https://chatgpt.com/share/688f138f-3658-8002-bd0a-b6d99852395c :- C Descipline
- 
- 

### ❓ Questions I Still Have
- 
- 
- 

###  What I Explored or Read
- Topic / Article / Video:
	- lua
		```lua 
print("hallo")
local v1,v2 = 22,33
local sum = v1 + v2
local subt = v1 - v2
local mul2 = v1 * v2
print("sum is :",sum)
print("sub is:",subt)
print("multiplication is:",mul2)
   ```
	   - is a simple program this is the basic syntax
	   - <span style="color:rgb(255, 0, 0)">2nd</span> 
	   - get input from user
	   ```lua
	  io.write("enter 2 values") -- here same effect as print ,but for user input io.write request is better
	  local v1 = io.read()
	  local v2 =io.read()
	  print("entered is",v1,v2)
	  local sum = v1+v2
	  
	  print("sum is",sum)
	   ```
		**that's how we can get input from user and print output
		- 

- Source / Link:
- Summary or Notes:

### Reflection / Thoughts
- How did I feel while learning today?
- What excited or frustrated me?

###  Next Curiosity / Deep Dive Ideas
- 
- 
### code snippets
use tags to refer
- #if_syntax
```lua
local num =60
if num > 50 then
    print("num is greater than 50")
elseif num ~= 50 then
    if num < 30 then
        print("number is less than 30")
    elseif num == 30 then
        print("number is 30")
    elseif num > 30 and num < 50 then
        print("num is between 30 to 50 range ")
    end
elseif num == 50 then
    print("number is exactly 50")
else 
    print("error")
end
```
- here is the basic if syntx of lua, also check the 'then end' that act as { ..........}
- 
```bash
cat $(find . -name "*.md" | gum choose)
```
here you can see a command that find all with extension .md 
	then we used  | for pipe that listed files to gum choose, 
	if we didn't write cat then if we choose something then no effect in that command
	so cat will get the return of the inner commands
	this is similar to all commands i mean the formate
	
#lua_star
```lua
io.write("enter")
local val=io.read("*n")
for i = 0 ,val do
    for j=1,i do
        io.write("*")
    end
    io.write("\n")
end
```