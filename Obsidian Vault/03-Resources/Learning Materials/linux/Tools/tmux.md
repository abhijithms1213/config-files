```bash
tmux attach-session -t name
```
- attach particular session
```inner commands
<C-space> + ? for list of commands
<C-space> + / for key on top for any command
<C-space> + : for `:` <- get it
```
