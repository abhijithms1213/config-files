# Appearance Configuration
# Look and Feel settings

# General appearance
general {
  gaps_in = 5
    gaps_out = 10
    border_size = 2

# Border colors
    col.active_border = rgba(245,245,245,1) rgba(0,0,0,0.2) 45deg
    col.inactive_border = rgba(0,0,0,0.2)

    resize_on_border = false
    allow_tearing = false
# layout = dwindle
    layout = master
}

# Window decoration
decoration {
  rounding = 2 
    rounding_power = 2

# Window opacity
    active_opacity = 1.0
    inactive_opacity = 1.0

# Shadow settings
    shadow {
      enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

# Blur settings
  blur {
    enabled = true
      size = 5
      passes = 3
      xray = true
      new_optimizations = true
  }
}

# Animations
animations {
  enabled = yes, please :)

# Custom bezier curves
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1
bezier = overshoot, 0.05, 0.9, 0.1, 1.1
# Animation definitions
    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear

animation = workspaces, 1, 1.3, easeInOutCubic, slidefade
animation = workspacesIn, 1, 1.3, overshoot, slidefade
animation = workspacesOut, 1, 1.3, easeInOutCubic, slidefade

# animation = workspaces, 1, 1.94, almostLinear, fade
# animation = workspacesIn, 1, 1.21, almostLinear, fade
# animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Layout configurations
dwindle {
  pseudotile = true
    preserve_split = true
}

master {
  new_status = master
}

# Miscellaneous
misc {
  force_default_wallpaper = -1
    disable_hyprland_logo = false
}
