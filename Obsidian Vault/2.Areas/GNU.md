gnu is a project , is focusing on creating a unix like os 
so gnu provided some uitilites and tools from it ,
- gcc compiler
- utilities like (ls,pwd etc)
- bash shell
- make, gdb. other useful tools also we got 
but issue is gnu lack in kernal department , because gnu only provided those tools uitilites etc 
so torvalds make linux and used gnu project's tools nd etc 
<span style="color:rgb(232, 125, 125)">and we got the powerfull linux system</span> 
that's why we call it as <span style="color:rgb(232, 125, 125)">GNU/linux</span> 
linux is only kernal 

actually GNU + linux is correctly the OS

When you use commands like:
```
ls
cp
gcc
bash
```
You’re using GNU software running on top of the Linux kernel.
pacman -Qu gcc